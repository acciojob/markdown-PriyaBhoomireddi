import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App"; // Adjust if the path is different

ReactDOM.render(<App />, document.getElementById("root")); // Ensure the correct root element is used
